import LandingPage from "./userPage/publicPage/page";

/**
 * Main page component that serves as the entry point for the application
 * Renders the public landing page component
 * @returns JSX element containing the landing page
 */
export default function MainPage() {
  return <LandingPage />;
}
